# Product Summary

ProofOfStudy is a learning check-in and education productivity mini app.

The user connects a wallet, claims one daily learning reward, and builds a public streak.
The first meaningful interaction shows immediate reward progress without requiring a token purchase.

The interface intentionally centers one primary action: `Claim Today's Reward`.
