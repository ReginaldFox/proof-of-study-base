# QA Checklist

- The page loads in English.
- The primary action opens the wallet dialog when disconnected.
- Coinbase Wallet can be selected directly.
- Browser Wallet opens the browser extension picker.
- A connected wallet on the wrong network can switch to Base.
- A check-in transaction includes the builder data suffix.
- The public page contains the `base:app_id` meta tag.
